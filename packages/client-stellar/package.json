{
  "name": "@stellar-x402/client-stellar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Client-side Stellar x402 helpers (payload construction, validation, encoding).",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "lint": "eslint src --ext .ts",
    "test": "tsx tests/x402.test.ts"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.1.1"
  },
  "devDependencies": {
    "tsup": "^8.1.0",
    "tsx": "^4.15.7",
    "typescript": "^5.6.3"
  }
}

